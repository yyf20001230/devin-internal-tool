# All-in-one instance: every tools/*.yaml in one image (internal-tools:dev) on NodePort 30080.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
labels:
  - pairs:
      app.kubernetes.io/instance: all
    includeSelectors: true
