apiVersion: v1
kind: ConfigMap
metadata:
  name: internal-tools-config
data:
  # auto = seed demo data only when the volume is empty; always = reseed on every start; never
  SEED: auto
  OPENAI_MODEL: gpt-4o-mini
  OPENAI_EMBEDDING_MODEL: text-embedding-3-small
