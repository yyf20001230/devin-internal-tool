apiVersion: v1
kind: Service
metadata:
  name: internal-tools
spec:
  type: NodePort
  selector:
    app: internal-tools
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
